# fci_engine

`fci_engine` is a Python package for an engineering-grade implementation of the
standard Fast Causal Inference (FCI) algorithm.

The package is intended to learn Partial Ancestral Graphs (PAGs) from
observational data when latent confounding may be present. The initial project
structure is in place; the FCI algorithm itself has not been implemented yet.
